#include <Command.h>
#include <string>
#include <vector>
#include <iostream>

Command::Command() {

}

bool Command::execute(std::string cString) {
    //if(args.size() != cArgs.size()) {
    //    return false;
    //}
}
